//Drake Young


void drawDYCredits(int x, int y, GLuint texid)
{
    //show vv picture
    static foat angle = 0.0f;
    float fx = float x;
    floafy = (float)y;
    double g = 0.0;
    g += sin(angle) * 10.0f;
    fx += g;
    fy += g + 2.0;
    angle += 0.5;
    glColor3ub(250
